#include<iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	for(int k=1;k<=t;k++)
	{
	    int n;
	    cin>>n;//size of array
	    int a[n];//IMP
	    for(int i=0;i<n;i++)
	    {
	        cin>>a[i];
	    }
	   /* int rev[n];
	    for(int i=n-1,j=0;i>=0;i--,j++)
	        rev[j]=a[i];
	    for(int i=0;i<n;i++)
	        cout<<rev[i]<<" ";
	   */
	   for(int i=n-1;i>=0;i--)
	        cout<<a[i]<<" ";
           cout<<"\n";
	}
	return 0;
}
